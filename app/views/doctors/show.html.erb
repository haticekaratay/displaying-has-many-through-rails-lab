<% @doctor.name %>
<% @doctor.appointments.each do |app|%>
<%= link_to app.patient.name, patient_path(app.patient_id) %> - <%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> <br>
<% end %>