<h2> <%= @patient.name %>'s page</h2>
<h4> Here is your list of appointments</h4>
<ul>
    <% @patient.appointments.each do |app| %>
    <li> 
    <%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> with doctor <%= link_to app.doctor.name, doctor_path(app.doctor_id)%>
    </li>
    <% end %>
<ul>